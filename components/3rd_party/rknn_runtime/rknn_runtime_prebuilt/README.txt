Place RKNN runtime files here for rk3566 builds.

Required layout:
- include/rknn_api.h
- lib/librknnrt.so

This runtime must match the target board BSP/NPU driver version
and target ABI (aarch64, glibc).
