Place prebuilt librk_lib.so here when Kconfig option
"Compile rk_lib from source" is disabled.

File name: librk_lib.so
Built on a matching toolchain / ABI (e.g. aarch64-linux-gnu, same glibc as target rootfs).
